pipeline { 

  agent {label 'Server-172'}
  
   tools {nodejs "npm"}
   
    stages{ 

      stage('git checkout'){ 
        steps { 
             dir('/var/opt/nest-tps-staging') {
              
           git credentialsId: 'PAT', url: 'https://git.stldev.io:1305/fullstack-team/nest-tps.git'

             }

              } 

         }  
}
}
